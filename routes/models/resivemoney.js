let balance = require('../functional/balance.js');module.exports=(req,cb)=>{    let amt= req.body.amt;    let t_p='Receive Money';    balance.addBal(req.session.userId,amt,t_p,(transOk)=>{        if(transOk === null){            cb('Error occurred in Adding Amount to the Receiver');        }else{            console.log('Amount Transferred');            cb(undefined,'Money Added Successfully');        }    });};